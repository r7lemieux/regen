eventPackagePluginStart = {
   // hack to ensure the generators are packaged in the plugin
   String dir = "$projectWorkDir/src"

   //ant.copy (todir: dir) {
	 //  fileset(dir:"$basedir/src/generators")
   //}
   
}
